Place `er_diagram.png` and `query_results.png` here.

The ER diagram itself is maintained as Mermaid source (not a static image)
in `README.md` and `docs/ERD.md` — export a PNG from there if you need a
static image for this folder.
